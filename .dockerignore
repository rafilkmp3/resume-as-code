# .dockerignore - Industry standard Docker context exclusions
# Keep Docker builds clean, fast, and secure

# Version control (largest space saver)
.git/
.gitignore
.gitattributes
.githooks/
*.git*

# Documentation and README files (not needed in container)
README.md
CHANGELOG.md
LICENSE
*.md
docs/

# IDE and editor files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
.editorconfig
.claude/
*.swp
*.swo
*~

# macOS system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride

# Windows system files
ehthumbs.db
Thumbs.db
Desktop.ini

# Linux system files
*~
.directory

# Dependencies (will be installed fresh inside container)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.npm/
.yarn-integrity
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Build artifacts and outputs (will be generated inside container)
dist/
build/
out/
temp/
*.pdf
*.zip
*.tar.gz
*.tgz
coverage/
.next/
.nyc_output/

# Test artifacts and reports (not needed for build)
test-results/
test-reports/
playwright-report/
playwright/.cache/
jest_*.json
*.lcov
lighthouse-reports/
*.lighthouse.json

# Logs and runtime data
logs/
*.log
pids/
*.pid
*.seed
*.pid.lock

# Environment and secrets (critical for Docker builds)
.env*
!.env.example
*.pem
*.key
secrets/
.secrets/
.aws/

# Cache directories
.cache/
.parcel-cache/
*.tsbuildinfo
.eslintcache
.stylelintcache

# Docker files (avoid recursive Docker builds - except our docker/ directory)
.dockerignore

# CI/CD configuration (not needed for build)
.github/
.gitlab-ci.yml
.travis.yml
.circleci/
.jenkins/

# Security scanning
.snyk
.trivyignore

# Terraform and infrastructure
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars

# Performance and monitoring
*.perf
*.cpuprofile

# Backup and temporary files
*.backup
*.bak
*.orig
*.tmp
*.temp

# Local development only
scripts/clean-local.sh
scripts/dev-*
.local/
local/
docker-compose.override.yml
.docker/

# Resume-as-Code specific exclusions
original-pdf-not-ai/
docs/screenshots/

# Keep essential files for container builds
!package.json
!package-lock.json
!yarn.lock
!config/
!Makefile
!resume-data.json
!templates/
!src/
!assets/images/
!scripts/build.js
!scripts/server.js
!scripts/dev-server.js
!scripts/utils/
!docker/
