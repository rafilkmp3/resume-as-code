# act configuration for resume-as-code
# Platform mappings for consistent local testing

# Use larger runner for comprehensive builds
-P ubuntu-latest=catthehacker/ubuntu:act-latest
-P ubuntu-22.04=catthehacker/ubuntu:act-22.04

# ARM64 GitHub runner simulation (NEW!)
-P ubuntu-24.04-arm=ubuntu:24.04
-P ubuntu-22.04-arm=ubuntu:22.04

# Container architecture for Apple Silicon (ARM64 native)
--container-architecture linux/arm64

# Default settings
--artifact-server-path /tmp/act-artifacts
--env-file .env.act

# GitHub token for API access (will be set via environment)
--secret-file .secrets.act
