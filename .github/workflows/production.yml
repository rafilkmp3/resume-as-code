name: Production Pipeline

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'assets/**'
      - 'config/**'
      - '*.html'
      - '*.json'
      - '*.js'
      - '*.css'
      - 'scripts/**'
      - 'docker/Dockerfile'
      - '.github/workflows/production.yml'
      - 'Makefile'
      - 'package*.json'
  workflow_dispatch:
    inputs:
      deployment_mode:
        description: 'Deployment mode'
        required: false
        default: 'normal'
        type: choice
        options:
          - normal
          - emergency
      emergency_reason:
        description: 'Emergency deployment reason (if emergency mode)'
        required: false
        type: string

concurrency:
  group: production-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write  # Changed from read to write for version bumping
  pages: write
  id-token: write
  packages: write
  pull-requests: write  # For GitHub Release creation

jobs:
  # Unified build job (handles both normal and emergency modes)
  # Note: Version management is fully handled by release-please workflow
  build:
    name: Build Resume (${{ github.event.inputs.deployment_mode || 'normal' }} mode)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      DOCKER_BUILDKIT: 1
      BUILDX_NO_DEFAULT_ATTESTATIONS: 1
      IS_EMERGENCY: ${{ github.event.inputs.deployment_mode == 'emergency' }}
      # GitHub Pages deployment environment variables
      NETLIFY: 'false'
      NETLIFY_ENV: 'github-pages'
      REVIEW_ID: ''
      HEAD: ${{ github.ref_name }}
      BRANCH: ${{ github.ref_name }}
      CONTEXT: 'production'
      DEPLOY_PRIME_URL: 'https://rafilkmp3.github.io/resume-as-code/'
      DEPLOY_URL: 'https://rafilkmp3.github.io/resume-as-code/'
      URL: 'https://rafilkmp3.github.io/resume-as-code/'
      # Version and release information
      NODE_ENV: 'production'
      GITHUB_PAGES: 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Calculate version and commit info
        id: version-info
        run: |
          echo "🔍 Calculating version and commit information..."

          # Get current version from package.json
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          echo "current-version=${CURRENT_VERSION}" >> $GITHUB_OUTPUT

          # Find the last release tag
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -z "$LAST_TAG" ]; then
            # No tags found, count all commits
            COMMITS_SINCE_RELEASE=$(git rev-list --count HEAD)
            echo "📊 No previous releases found"
            echo "📊 Total commits: ${COMMITS_SINCE_RELEASE}"
          else
            # Count commits since last tag
            COMMITS_SINCE_RELEASE=$(git rev-list --count ${LAST_TAG}..HEAD)
            echo "📊 Last release: ${LAST_TAG}"
            echo "📊 Commits since release: ${COMMITS_SINCE_RELEASE}"
          fi

          echo "commits-since-release=${COMMITS_SINCE_RELEASE}" >> $GITHUB_OUTPUT

          # Export for use in build
          echo "APP_VERSION=${CURRENT_VERSION}" >> $GITHUB_ENV
          echo "COMMITS_SINCE_RELEASE=${COMMITS_SINCE_RELEASE}" >> $GITHUB_ENV
          echo "LAST_RELEASE_TAG=${LAST_TAG}" >> $GITHUB_ENV

      - name: Detect build context
        id: context
        run: |
          echo "🔍 Detecting build context..."

          # Detect build context and generate appropriate URLs
          REPO_URL="https://github.com/${{ github.repository }}"

          if [[ "${{ github.event_name }}" == "release" ]]; then
            echo "BUILD_CONTEXT=release" >> $GITHUB_ENV
            echo "CONTEXT_URL=${{ github.event.release.html_url || format('{0}/releases/tag/{1}', env.REPO_URL, github.ref_name) }}" >> $GITHUB_ENV
            echo "📊 Build context: Release"
          elif [[ "${{ github.event_name }}" == "pull_request" ]]; then
            echo "BUILD_CONTEXT=pr" >> $GITHUB_ENV
            echo "PR_NUMBER=${{ github.event.pull_request.number }}" >> $GITHUB_ENV
            echo "CONTEXT_URL=${{ github.event.pull_request.html_url }}" >> $GITHUB_ENV
            echo "📊 Build context: Pull Request #${{ github.event.pull_request.number }}"
          elif [[ "${{ github.ref_name }}" == "main" ]]; then
            echo "BUILD_CONTEXT=main" >> $GITHUB_ENV
            echo "CONTEXT_URL=${REPO_URL}/commits/main" >> $GITHUB_ENV
            echo "📊 Build context: Main branch"
          else
            echo "BUILD_CONTEXT=branch" >> $GITHUB_ENV
            echo "CONTEXT_URL=${REPO_URL}/tree/${{ github.ref_name }}" >> $GITHUB_ENV
            echo "📊 Build context: Branch ${{ github.ref_name }}"
          fi

          # Generate compare URL for commits ahead
          if [ -n "$LAST_RELEASE_TAG" ]; then
            echo "COMPARE_URL=${REPO_URL}/compare/${LAST_RELEASE_TAG}...HEAD" >> $GITHUB_ENV
          else
            echo "COMPARE_URL=${REPO_URL}/commits/${{ github.ref_name }}" >> $GITHUB_ENV
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Emergency deployment notice
        if: env.IS_EMERGENCY == 'true'
        run: |
          echo "# 🚨 EMERGENCY DEPLOYMENT" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Reason:** ${{ github.event.inputs.emergency_reason }}" >> $GITHUB_STEP_SUMMARY
          echo "**Mode:** Maximum speed, no optimization, no testing" >> $GITHUB_STEP_SUMMARY
          echo "**Risk:** High - use only when production is broken" >> $GITHUB_STEP_SUMMARY

          echo "🚨 EMERGENCY DEPLOYMENT: ${{ github.event.inputs.emergency_reason }}"

      - name: Inject version into documentation
        if: env.IS_EMERGENCY != 'true'
        run: |
          if [ -f "scripts/inject-version.js" ]; then
            node scripts/inject-version.js || echo "⚠️ Version injection failed, continuing"
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          platforms: linux/amd64

      - name: Log in to Container Registry for cache
        if: env.IS_EMERGENCY != 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Cache Docker layers
        if: env.IS_EMERGENCY != 'true'
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ hashFiles('package*.json', 'docker/Dockerfile') }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Optimized Docker build (normal mode)
        if: env.IS_EMERGENCY != 'true'
        run: |
          echo "🏗️ Normal mode: Optimized build with registry + local caching"
          docker buildx build \
            --file docker/Dockerfile \
            --target builder \
            --platform linux/amd64 \
            --build-arg GITHUB_SHA=${{ github.sha }} \
            --build-arg GITHUB_REF_NAME=${{ github.ref_name }} \
            --build-arg APP_VERSION=${APP_VERSION} \
            --build-arg COMMITS_SINCE_RELEASE=${COMMITS_SINCE_RELEASE} \
            --build-arg LAST_RELEASE_TAG=${LAST_RELEASE_TAG} \
            --build-arg BUILD_CONTEXT=${BUILD_CONTEXT} \
            --build-arg PR_NUMBER=${PR_NUMBER} \
            --build-arg CONTEXT_URL="${CONTEXT_URL}" \
            --build-arg COMPARE_URL="${COMPARE_URL}" \
            --build-arg NODE_ENV=production \
            --build-arg GITHUB_PAGES=true \
            --build-arg NETLIFY=false \
            --build-arg NETLIFY_ENV=github-pages \
            --build-arg CONTEXT=production \
            --build-arg DEPLOY_URL=https://rafilkmp3.github.io/resume-as-code/ \
            --cache-from type=registry,ref=ghcr.io/${{ github.repository }}-cache:golden-base \
            --cache-from type=registry,ref=ghcr.io/${{ github.repository }}-cache:golden-base-${{ github.ref_name }} \
            --cache-from type=registry,ref=ghcr.io/${{ github.repository }}-cache:builder \
            --cache-from type=registry,ref=ghcr.io/${{ github.repository }}-cache:builder-${{ github.ref_name }} \
            --cache-from type=local,src=/tmp/.buildx-cache \
            --cache-to type=registry,ref=ghcr.io/${{ github.repository }}-cache:golden-base,mode=max \
            --cache-to type=registry,ref=ghcr.io/${{ github.repository }}-cache:golden-base-${{ github.ref_name }},mode=max \
            --cache-to type=registry,ref=ghcr.io/${{ github.repository }}-cache:builder,mode=max \
            --cache-to type=registry,ref=ghcr.io/${{ github.repository }}-cache:builder-${{ github.ref_name }},mode=max \
            --cache-to type=local,dest=/tmp/.buildx-cache-new,mode=max \
            --tag resume:build-${{ github.sha }} \
            --load \
            .

          # Update local cache atomically (fallback for registry cache)
          if [[ -d "/tmp/.buildx-cache-new" ]]; then
            rm -rf /tmp/.buildx-cache
            mv /tmp/.buildx-cache-new /tmp/.buildx-cache
          fi

      - name: Emergency Docker build (emergency mode)
        if: env.IS_EMERGENCY == 'true'
        run: |
          echo "🚨 Emergency mode: Fast build, no cache, no optimization"
          docker buildx build \
            --file docker/Dockerfile \
            --target builder \
            --platform linux/amd64 \
            --build-arg GITHUB_SHA=${{ github.sha }} \
            --build-arg GITHUB_REF_NAME=${{ github.ref_name }} \
            --build-arg APP_VERSION=${APP_VERSION} \
            --build-arg COMMITS_SINCE_RELEASE=${COMMITS_SINCE_RELEASE} \
            --build-arg LAST_RELEASE_TAG=${LAST_RELEASE_TAG} \
            --build-arg BUILD_CONTEXT=${BUILD_CONTEXT} \
            --build-arg PR_NUMBER=${PR_NUMBER} \
            --build-arg CONTEXT_URL="${CONTEXT_URL}" \
            --build-arg COMPARE_URL="${COMPARE_URL}" \
            --build-arg NODE_ENV=production \
            --build-arg GITHUB_PAGES=true \
            --build-arg NETLIFY=false \
            --build-arg NETLIFY_ENV=github-pages \
            --build-arg CONTEXT=production \
            --build-arg DEPLOY_URL=https://rafilkmp3.github.io/resume-as-code/ \
            --tag resume:build-${{ github.sha }} \
            --no-cache \
            --load \
            .

      - name: Extract build artifacts
        run: |
          mkdir -p dist
          docker run --rm -v "$PWD/dist:/tmp/dist" resume:build-${{ github.sha }} sh -c "cp -r /app/dist/* /tmp/dist/"
          echo "📁 Build artifacts:"
          ls -la dist/

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: production-build-${{ github.sha }}
          path: dist/
          retention-days: ${{ env.IS_EMERGENCY == 'true' && '90' || '30' }}
          compression-level: ${{ env.IS_EMERGENCY == 'true' && '1' || '6' }}

  # Quality Gates (comprehensive validation before deployment)
  quality-gates:
    name: Quality Gates (${{ matrix.gate-type }})
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: build
    if: github.event.inputs.deployment_mode != 'emergency'
    strategy:
      fail-fast: false
      matrix:
        gate-type: [functionality, security, performance]
    env:
      CI: true
      NODE_ENV: production
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        continue-on-error: true
        with:
          lfs: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
        continue-on-error: true

      - name: Install dependencies
        run: npm ci --prefer-offline --no-audit || echo "Dependency installation failed"
        continue-on-error: true

      - name: Run quality gate
        run: |
          echo "🚧 Quality Gate: ${{ matrix.gate-type }} validation"
          case "${{ matrix.gate-type }}" in
            functionality)
              echo "🔧 Testing core functionality (profile image, navigation, theme switching)"
              npx playwright test tests/smoke-functionality.spec.js --project=chromium || echo "❌ Functionality gate failed (non-blocking)"
              ;;
            security)
              echo "🔒 Security vulnerability scan"
              npm audit --audit-level=high --prefer-offline || echo "❌ Security vulnerabilities found (non-blocking)"
              ;;
            performance)
              echo "⚡ Performance validation"
              npm run test:unit || echo "❌ Performance tests failed (non-blocking)"
              echo "📊 Build size check"
              ls -lah dist/ | grep -E '\.(css|js|html)$' || echo "Build artifacts checked"
              ;;
          esac
        continue-on-error: true

      - name: Upload test results
        if: always() && hashFiles('coverage/**/*', 'test-results/**/*') != ''
        uses: actions/upload-artifact@v4
        with:
          name: quality-gate-${{ matrix.gate-type }}-results-${{ github.sha }}
          path: |
            coverage/
            test-results/
          retention-days: 7
        continue-on-error: true

  # Production deployment (guaranteed to work)
  deploy:
    name: Deploy to Production
    if: github.ref == 'refs/heads/main'
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: production-build-${{ github.sha }}
          path: dist/

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload to Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Deployment summary
        run: |
          DEPLOY_MODE="${{ github.event.inputs.deployment_mode || 'normal' }}"

          if [ "$DEPLOY_MODE" = "emergency" ]; then
            echo "# 🚨 EMERGENCY DEPLOYMENT COMPLETE" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Emergency Reason:** ${{ github.event.inputs.emergency_reason }}" >> $GITHUB_STEP_SUMMARY
            echo "**Deployment Type:** 🚨 EMERGENCY - No testing, maximum speed" >> $GITHUB_STEP_SUMMARY
          else
            echo "# 🚀 Production Deployment Complete" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Deployment Type:** Normal production pipeline" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 🌐 Live Resume" >> $GITHUB_STEP_SUMMARY
          echo "### 📱 [**View Resume**](${{ steps.deployment.outputs.page_url }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** [\`${{ github.sha }}\`](${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }})" >> $GITHUB_STEP_SUMMARY
          echo "**Actor:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY

          if [ "$DEPLOY_MODE" = "emergency" ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "## ⚠️ Post-Emergency Actions" >> $GITHUB_STEP_SUMMARY
            echo "1. 🔍 Verify the issue is resolved" >> $GITHUB_STEP_SUMMARY
            echo "2. 🧪 Run staging tests when safe" >> $GITHUB_STEP_SUMMARY
            echo "3. 📋 Document emergency cause" >> $GITHUB_STEP_SUMMARY
            echo "4. 🔄 Use normal pipeline for next changes" >> $GITHUB_STEP_SUMMARY
          fi
